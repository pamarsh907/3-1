{
  "name": "backend",
  "version": "0.0.1",
  "description": "\"\"",
  "license": "ISC",
  "author": "Peter Marsh",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "build:ui": "rm -rf dist && cd ../part2/part2-notes-frontend && npm run build && cp -r dist ../../3-1",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint .",
    "test": "cross-env NODE_ENV=test node --test"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.7.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "supertest": "^7.2.2"
  }
}
